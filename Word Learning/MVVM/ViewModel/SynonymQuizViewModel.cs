using System.Windows;

namespace Word_Learning.MVVM.ViewModel
{
    public class SynonymQuizViewModel
    {
        private Window window;

        public SynonymQuizViewModel(Window window)
        {
            this.window = window;
        }
    }
}
